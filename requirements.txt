# Core requirements for cdata-codegen project
# Python 3.11+ / CCP4-10 Compatible

# Django for database and web framework
Django>=5.2

# BioPython for sequence file parsing and biological data
biopython>=1.80

# System and process utilities
psutil>=5.9.0

# XML processing
lxml>=4.9.0

# Crystallography library
gemmi>=0.6.0

# Testing framework
pytest>=7.4.0
pytest-django>=4.5.0
pytest-asyncio>=0.21.0
pytest-xdist>=3.3.0  # Parallel test execution

# Code formatting and linting
autopep8>=2.0.0

# Django REST Framework for API
djangorestframework>=3.14.0

# Django extensions and utilities
django-cors-headers>=4.0.0
django-filter>=23.0
whitenoise>=6.5.0
pytz>=2023.3

# Type checking
mypy>=1.0.0

# Network/graph analysis
networkx>=3.0

# HTTP requests
requests>=2.31.0

# YAML support
PyYAML>=6.0

# For async database operations
asgiref>=3.7.0

# Scientific computing (required by some crystallography plugins)
# IMPORTANT: NumPy must be < 2.0 for CCP4 compatibility
numpy<2,>=1.24.0
scipy>=1.16.0

# Data analysis (required by some plugins like servalcat_pipe)
pandas>=2.0.0

# Chemistry toolkit (required for ligand/molecule handling)
rdkit>=2023.3.1

# SVG generation (required by iris_validation and other reporting plugins)
svgwrite>=1.4.0

# File format support
chardet>=5.0.0  # Character encoding detection
mrcfile>=1.4.0  # MRC/CCP4 electron density map files

# CCTBX/iotbx - Computational Crystallography Toolbox
# Now pip-installable as of CCP4-10 / cctbx 2025.10+
cctbx-base>=2025.10

# Visualization (required by cctbx and reporting)
matplotlib>=3.10

# CCP4-Specific Packages (symlinked from CCP4-10, not pip-installed):
# - clipper: Crystallographic library (symlinked from CCP4-10)
# - iris_validation: Protein validation metrics (symlinked from CCP4-10)
# - pyrvapi: Report Viewing API (symlinked from CCP4-10)
# - ccp4mg: CCP4 molecular graphics library (includes mmut module)
# - phaser: Phasing software package
# - mrbump: Molecular replacement pipeline
#
# To symlink these modules:
#   ln -sf /path/to/ccp4-10/.../site-packages/{clipper.py,_clipper.so,iris_validation,pyrvapi.so,pyrvapi_ext,ccp4mg,phaser,mrbump} .venv/lib/python3.11/site-packages/
#
# See DEVELOPMENT_SETUP.md for detailed symlinking instructions
